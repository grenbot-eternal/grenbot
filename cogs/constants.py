#!/usr/bin/env python
import os
from datetime import timedelta

# Collection of shared top level variables

CHANNELS = ["kpop", "insomnias", "bot-channel", "bot-nsfw", "simp-world"]
PIC_DIR = os.path.join("D:", "Dropbox", "JYP sixteen")
TIMEZONE_DELTA = timedelta(hours=9)


def main():
    return


if __name__ == "__main__":
    main()
